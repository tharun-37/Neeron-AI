// The Cloudflare gatekeeper currently provides authentication only — it exposes no resource types
// for gadgets/agents yet. Capabilities (Workers logs, R2, etc.) will add their Session types here.

